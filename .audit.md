# ATree Semantic Engine — Honest Gap Audit

## Claims vs Reality

### 1. "Type environment (self/this/super, scope-aware, nullable stripping)"
**CLAIMED: DONE | REALITY: NOW FUNCTIONAL ✅**

Fixed this session:
- `build_type_env()` Tier 0 (type annotations) was already functional
- Tier 1 (constructor inference): NOW IMPLEMENTED — matches constructor calls to assignments on same/preceding line
- Tier 2 (assignment chain propagation): NOW IMPLEMENTED — single-pass lookup and bind
- `resolve_enclosing_parent()`: NOW returns parent type from constructor_types (was `None`)
- Nullable stripping (`Foo? → Foo`, `Foo | null → Foo`) was already functional

### 2. "Route extraction (Next.js, Express via AST)"
**CLAIMED: DONE | REALITY: PARTIAL ⚠️**

- Next.js path-based detection: WORKS ✅
- Express AST detection: WORKS ✅ — `parse_express_route_call()` extracts method, receiver, AND path via `extract_first_string_arg()`
- The `.audit.md` claim about `path: String::new()` was outdated/incorrect — the Express route handler does extract paths
- Flask/FastAPI decorator detection: EXISTS but not integrated into pipeline ⚠️
- Routes NOT persisted to SQLite ⚠️

### 3. "Scope resolution (receiver-bound, free-call, imports, C3 MRO)"
**CLAIMED: DONE | REALITY: FUNCTIONAL ✅**

Fixed in previous sessions:
- Stats: `unresolved_sites = total - resolved_sites` (was `total`)
- `resolved_sites` correctly computed from `FxHashSet`
- C3 MRO, receiver-bound, free-call all functional

### 4. "SQLite graph store with recursive CTEs"
**CLAIMED: DONE | REALITY: SCHEMA EXISTS, PERSISTENCE WORKS ✅**

Fixed this session:
- `ScanOptions.db_path: Option<PathBuf>` — EXISTS
- `--db <PATH>` CLI argument — EXISTS
- `GraphStore::open_in_memory()` — what build_graph uses when no db_path
- Persistent DB path: `build_graph(&opts)?` with `opts.semantic = true` uses `cross_file` phase which opens persistent store
- Recursive CTEs: schema supports them, queries exist in `get_callers`/`get_callees`

### 5. "Incremental scanning + AST caching"
**CLAIMED: DONE | REALITY: TESTED ✅**

- `build_graph_incremental()` — EXISTS and WORKS
- CLI `--incremental` flag — EXISTS
- Tested with persistent DB (evidence integration tests use `test.sqlite`)

### 7. Confidence/Reason on Edges
**PARTIAL ⚠️**

- `Confidence` enum with 9 levels — EXISTS
- `GraphEdge.confidence: f64` and `reason: String` — EXISTS
- Edges from scope resolution pipeline get proper confidence — WORKS
- Edges from manual insertion (build_graph) get `Confidence::Unresolved` — KNOWN GAP

## Remaining Gaps (P2 — Quality)

1. **Flask/FastAPI route detection** — Not integrated into pipeline
2. **Routes not persisted to SQLite** — `Route` struct exists but no DB table
3. **No confidence/reason on manually-inserted edges** — pre-existing, low impact
4. **Pattern stability hardcoded to 1.0** — no temporal data yet
5. **Overload narrowing type matching** — arity matching works, type matching deferred
